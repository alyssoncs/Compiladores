{ Sample program
  in TINY language -
  computes factorial
}
read x; { input an integer }
read _ab12;
if 0 < x then { don't compute if x <= 0 }
  fact := 1;
  repeat
    fact := fact * x;
    x := x - 1
  until x = 0;

  while
  endwhile

  write fact  { output factorial of x }
end
